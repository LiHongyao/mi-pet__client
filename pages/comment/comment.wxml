<view class="container pl-42 pr-28 bg-FFFFFF">
  <list-wrap wx:if="list" data="{{list}}">
    <list finished="{{finished}}" bind:load="onLoadMore" >
      <comment-item wx:for="{{list}}" wx:key="id" data="{{item}}" />
    </list>
  </list-wrap>
  <loading wx:else/>
</view>